// import React from 'react';
// import MapMarker from '../map/MapMarkers'; // Adjust the import based on your file structure

// const MapStoppagePoints = ({ stoppagePoints, onMarkerClick }) => {
//   return (
//     <>
//       {stoppagePoints.map((point) => (
//         <MapMarker
//           key={point.id}
//           position={point}
//           title={point.title}
//           onClick={() => onMarkerClick(point.id)}
//         />
//       ))}
//     </>
//   );
// };

// export default MapStoppagePoints;
